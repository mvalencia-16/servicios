# CORS: en Render configurar la variable de entorno CORS_ALLOWED_ORIGINS
# con la URL del frontend (ej: https://demo-hola-frontend.onrender.com)
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}

server.port=${PORT:8080}

